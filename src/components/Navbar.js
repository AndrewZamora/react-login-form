import React from 'react';

const style ={
    margin: '0',
    paddingLeft:'1em'
}

const Navbar = () => (
    <nav>
        <p style={style}>Basic React Login Form</p>
    </nav>
);

export default Navbar;
